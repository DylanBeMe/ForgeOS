Optional desktop-generated files:
  metadata.tsv          path, display title, 64x80 BMP artwork path
  art/*.bmp             preprocessed 64x80 box art

Use tools/build-library-index.py on a desktop, then copy its output into this
folder. Per-game overrides belong in /mnt/forgeshell/state/game-overrides.tsv.
